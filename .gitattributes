# Keep shell scripts LF so they run inside Linux containers even when the
# repo is checked out on Windows (avoids CRLF breaking the shebang).
*.sh text eol=lf
docker-entrypoint.sh text eol=lf

# Normalize line endings for common text files.
* text=auto
