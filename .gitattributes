# Normalize line endings. Shell scripts and files that end up inside the
# Linux container MUST stay LF even when checked out on Windows.
* text=auto

*.sh text eol=lf
Dockerfile text eol=lf
docker-compose*.yml text eol=lf
*.mjs text eol=lf

# Binary assets
*.png binary
*.jpg binary
*.ico binary
*.db binary
